# requirements.txt
# Dependencies for scripts/update_catalog.py
#
# Install with:
#   pip install -r scripts/requirements.txt
#
# You also need ast-grep on PATH:
#   cargo install ast-grep
#   (or download from https://ast-grep.github.io/guide/quick-start.html)

anthropic>=0.40.0
pyyaml>=6.0
